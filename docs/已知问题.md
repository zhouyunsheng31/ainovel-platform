# AI小说拆书系统 - 已知问题

## 文档信息
| 项目 | 内容 |
|------|------|
| 文档名称 | 已知问题 |
| 版本 | v1.0.0 |
| 创建日期 | 2026-04-01 |
| 适用范围 | 问题追踪与后续优化 |

---

## 一、低严重度问题（不影响MVP发布）

| 编号 | 问题描述 | 严重度 | 影响范围 | 建议修复版本 |
|------|----------|--------|----------|-------------|
| L-001 | 上传进度为模拟值 | 低 | 前端上传界面 | v1.1.0 |
| L-002 | WebSocket 与轮询同时运行 | 低 | 进度监控 | v1.1.0 |
| L-003 | 导出按钮无事件绑定 | 低 | 详情页导出Tab | v1.1.0 |
| L-004 | 纯数字标题行匹配不完整 | 低 | 文本预处理 | v1.1.0 |

---

## 二、兼容性警告（非阻断，当前可用）

| 编号 | 警告来源 | 警告类型 | 严重度 | 说明 | 处理策略 |
|------|----------|----------|--------|------|----------|
| C-001 | LangChain (langchain-core 1.2.19) | UserWarning | ⚠️ 非阻断 | `Core Pydantic V1 functionality isn't compatible with Python 3.14 or greater.` — 来自 `pydantic.v1.fields.FieldInfo` 的导入 | 等待 LangChain 官方移除 pydantic.v1 依赖；当前版本可正常工作，但 Pydantic V3 发布后将变为阻断；关注 langchain-core 更新日志 |
| C-002 | 项目代码 `app/config.py:44` | PydanticDeprecatedSince20 | ⚠️ 非阻断 | `Support for class-based config is deprecated, use ConfigDict instead.` — 使用 `class Config:` 而非 `model_config = ConfigDict(...)` | 将 `class Config:` 迁移为 `model_config = SettingsConfigDict(...)`；改动量小，可在后续版本一并处理 |
| C-003 | 项目代码 `app/models/schemas.py:64` | DeprecationWarning | ⚠️ 非阻断 | `datetime.datetime.utcnow() is deprecated` — `APIResponse.timestamp` 使用 `datetime.utcnow()` | 替换为 `datetime.now(datetime.UTC)`；改动量小，可在后续版本一并处理 |
| C-004 | SQLAlchemy 2.0.48 内部 | DeprecationWarning | ⚠️ 非阻断 | `datetime.datetime.utcnow() is deprecated` — SQLAlchemy 内部 `server_default=func.now()` 触发 | 等待 SQLAlchemy 后续版本修复；当前版本不影响功能；无需项目代码改动 |
| C-005 | requests 2.32.5 | RequestsDependencyWarning | ⚠️ 非阻断 | `urllib3 (2.6.3) or chardet (7.2.0)/charset_normalizer (3.4.4) doesn't match a supported version!` | 升级 requests 至最新版或锁定 urllib3/chardet 兼容版本；当前不影响功能 |

---

## 三、环境相关问题

| 编号 | 问题描述 | 影响 | 解决建议 |
|------|----------|------|----------|
| E-001 | Android SDK 缺失 | 无法在当前环境完成完整 APK 构建 | 在具备完整 Android 开发环境的机器上进行最终构建 |
| E-002 | 首屏加载略超2秒（file://协议） | 移动端离线访问体验略受影响 | 部署到HTTP服务器预计<2s |
| E-003 | CORS 完全开放（allow_origins=["*"]） | 生产环境存在安全风险 | 生产环境应限制来源域名 |

---

## 四、功能增强建议（后续迭代）

| 编号 | 功能建议 | 优先级 | 说明 |
|------|----------|--------|------|
| F-001 | 用户认证体系 | P2 | MVP不包含，后续迭代 |
| F-002 | 历史记录管理 | P2 | 书库功能增强 |
| F-003 | 导出功能（Word/PDF） | P2 | 需求中提到，当前仅复制功能 |
| F-004 | 暗色主题完整支持 | P3 | UI体验增强 |
| F-005 | 移动端触摸交互优化 | P3 | UI体验增强 |
| F-006 | 骨架屏加载状态 | P3 | UI体验增强 |
| F-007 | 更多微交互动画 | P3 | UI体验增强 |
| F-008 | 无障碍ARIA标签 | P3 | 可访问性增强 |
| F-009 | LangSmith监控集成 | P3 | 可观测性增强 |
| F-010 | 任务队列（Celery/RQ） | P3 | 性能增强 |

---

## 五、问题修复记录

| 日期 | 修复问题 | 修复文件 | 说明 |
|------|----------|----------|------|
| 2026-03-24 | Toast消息系统BUG | css/styles.css、index.html | 修复 slideOut 动画、容器ID、info样式 |
| 2026-03-24 | UI设计简陋 | css/styles.css | 新增完整表单样式系统、导航与头部样式、页面布局样式、书库样式、设置页面样式、详情页面样式 |
| 2026-03-24 | ES Modules兼容性 | js/bundle.js、index.html | Android WebView在file://协议下不支持ES Modules，创建bundle.js |
| 2026-03-31 | 世界纲树形渲染BUG | js/app.js | worldOutline传入前增加Array.isArray判断并包装为数组 |
| 2026-03-31 | 列表视图补全 | js/app.js | renderOutlineList重构为遍历sections数组，依次渲染世界纲/大纲/粗纲/章纲 |
| 2026-03-31 | 真实API联调 | js/api.js | 上传路径修正、错误响应解析、状态映射补全、纲树响应解析、复制纲接口、API便捷方法统一提取res.data |
| 2026-03-31 | WebSocket联调 | js/app.js、js/api.js | WebSocket URL派生、消息类型补全、重连状态同步、outline_update事件订阅、wsConnected状态更新 |
| 2026-03-31 | 纲树层级修复 | backend/app/services/task_processor.py | _save_outlines()新增parent_outline_id设置，建立完整父子层级关系 |
| 2026-03-31 | 书库页搜索过滤 | js/app.js | handleSearch()和handleFilter()从空壳改为实际过滤逻辑 |

---

## 六、总结

| 类别 | 数量 | 说明 |
|------|------|------|
| 阻断级问题 | 0 | 当前无阻断级问题，MVP可正常发布 |
| 低严重度问题 | 4 | L-001~L-004，不影响核心功能 |
| 兼容性警告 | 5 | C-001~C-005，均为弃用告警，当前功能正常 |
| 环境相关问题 | 3 | E-001~E-003，与部署环境相关 |
| 功能增强建议 | 10 | F-001~F-010，后续迭代规划 |

**总体评估**：✅ **MVP可正常发布，已知问题均不影响核心功能使用**
